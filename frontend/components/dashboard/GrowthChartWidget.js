import React from 'react';
import WidgetCard from './WidgetCard';

const GrowthChartWidget = () => {
  return (
    <WidgetCard title="성장 곡선 (준비 중)">
      <p>목표 달성률, 감정 변화 등의 추이를 그래프로 보여줍니다.</p>
    </WidgetCard>
  );
};

export default GrowthChartWidget;
